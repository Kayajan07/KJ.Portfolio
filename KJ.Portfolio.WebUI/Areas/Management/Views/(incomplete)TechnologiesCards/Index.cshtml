@* @model ICollection<TechnologiesCard>

<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">Referans Kartları</h5>
        <a class="btn btn-primary float-end" asp-action="Create">Yeni Ekle</a>
    </div>
    <div class="card-body">

        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">Başlık</th>
                    <th scope="col">İçerik</th>
                    <th scope="col">İkon Sınıfı</th>
                    <th scope="col">İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Title</td>
                        <td>@item.Description</td>
                        <td>@item.IconClass</td>
                        <td>
                            <a class="btn btn-warning" 
                            asp-action="Edit" 
                            asp-route-id="@item.Id">Düzenle</a>

                            <button class="btn btn-danger" onclick='Delete("@(item.Id)", this)'>Sil</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

    </div>


@section PageScripts{
    <script>
        function Delete(id, elem){
            $.ajax({
                url: "/Management/TechnologiesCards/Delete/" + id,
                method: "POST",
                success: function(s){
                   if(s.success == true){
                       $(elem).closest("tr").remove();
                   }
                },
                error: function(){
                    alert("Bir sorun oluştu!");
                }
            })
        }
    </script>
} *@